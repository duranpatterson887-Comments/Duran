---
title: "CoinMarketCap Telegram Bot — Market Data in Your Chat"
sidebarTitle: "Telegram Bot"
description: "Check live crypto prices, top market rankings, and instant currency conversions directly inside Telegram with @CoinMarketCapBot — no app switching needed."
---

The CoinMarketCap Telegram bot puts real-time market intelligence at your fingertips wherever you trade, talk, and research. Pull live cryptocurrency prices, spot trending tokens before they break out, and convert between currencies — all without leaving your conversation. Whether you're in a group trading discussion or a one-on-one chat, @CoinMarketCapBot delivers the data you need the moment the market moves.

## Getting Started

<Steps>
  <Step title="Find the bot on Telegram">
    Open Telegram on any device and search for **@CoinMarketCapBot** in the search bar. Tap the official CoinMarketCap bot from the results list.
  </Step>
  <Step title="Start the bot">
    Tap **Start** or send the `/start` command. The bot will introduce itself and display a quick-start menu of available commands.
  </Step>
  <Step title="Query your first asset">
    Type any cryptocurrency ticker symbol — for example, `BTC`, `ETH`, or `SOL` — and the bot will return the current price, 24h change, and market cap instantly.
  </Step>
</Steps>

## Commands Reference

| Command | Description |
|---------|-------------|
| `/start` | Initialize the bot and display the welcome message with available commands. |
| `/price [SYMBOL]` | Get the current price and 24-hour percentage change for a cryptocurrency (e.g., `/price BTC`). |
| `/top` | Display the top 10 cryptocurrencies ranked by market capitalization. |
| `/trending` | Show the currently trending tokens based on CoinMarketCap watchlists and page views. |
| `/convert [AMOUNT] [FROM] [TO]` | Convert a value between any two currencies (e.g., `/convert 1.5 ETH USD`). |
| `/help` | List all available bot commands with usage examples. |

## Inline Mode

You don't need to open a private chat with the bot to use it. Inline mode lets you share live market data in **any Telegram conversation** — group chats, channels, or direct messages — making it easy to back your trading calls with real-time facts.

To use inline mode:

1. In any chat's message box, type `@CoinMarketCapBot` followed by a space and a ticker symbol (e.g., `@CoinMarketCapBot BTC`).
2. A live price card will appear above the keyboard as an inline result.
3. Tap the card to post it directly into the conversation.

The shared card includes the current price, 24h change, and a direct link to the full CoinMarketCap asset page — giving your whole group the context they need to make informed decisions.

<Warning>
  Inline results are fetched in real time. Make sure your Telegram app has an active internet connection to receive up-to-date data.
</Warning>

<Tip>
  Use the Telegram bot for quick on-the-go price checks, instant conversions, and sharing live market data with your trading community. For automated workflows, scheduled reports, and application back-end data needs, pair it with the [CoinMarketCap REST API](/api-reference/overview) — the two tools complement each other perfectly.
</Tip>
